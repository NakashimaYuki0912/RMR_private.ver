# 验收清单：Hub / 玩法介绍 / 图鉴 加固版

**Build 戳（必须一致）：** `2026-07-12Thub-help-atlas-hardening+08:00`  

**部署位置：**  
`E:\Steam\steamapps\workshop\content\1256670\3503523710\Assemblies\dlls\RogueLike Mod Reborn.dll`

**日志：**  
`%USERPROFILE%\AppData\LocalLow\Project Moon\LibraryOfRuina\Player.log`  
搜索：`[RMR] RogueLike Mod Reborn initializing. Build:`

---

## 0. 启动前

| # | 步骤 | 通过标准 |
|---|---|---|
| 0.1 | 完全退出 Library of Ruina（任务管理器无进程） | 无残留进程 |
| 0.2 | 启动游戏，进入中文 | 语言 cn |
| 0.3 | 打开 `Player.log` 确认 Build 戳 | 含 `hub-help-atlas-hardening` |
| 0.4 | 进入 RMR 邀请 / 开局 Hub | 能看到全屏开局页 |

---

## 1. Hub（开局页）

| # | 操作 | 通过标准 | 失败时注意 |
|---|---|---|---|
| 1.1 | 观察布局 | 左侧标题/说明，右侧编号菜单（I–VII 类票券按钮） | 不是旧居中单栏笔刷按钮也算本版预期 |
| 1.2 | 中文可读 | 无口口口、无明显糊成一团 | 对照 Noto 字体日志 |
| 1.3 | 悬停按钮 | 按钮轻微右移 / 高亮，可点 | 无 NRE |
| 1.4 | 有存档时 | 显示「继续旅程」 | 无存档则不显示 |
| 1.5 | 点「正常游玩」 | 发出邀请并进入流程（不立刻回 Hub） | 日志无 Rollback 误伤 |
| 1.6 | 再进 Hub，点「挑战解放战」 | 进入选层页；Hub soft-hide | 取消选层应回到 Hub |
| 1.7 | 选层后 | 进入解放编队/战斗路径 | 未选层不能误发邀请 |
| 1.8 | 「重置永久进度」 | 弹出确认；取消关闭弹层；确认有重置日志 | 不会误关整个游戏 |
| 1.9 | 「退出」 | 确认后回图书馆；无全黑挡点击 | Overlay 销毁 |

---

## 2. 玩法介绍

| # | 操作 | 通过标准 |
|---|---|---|
| 2.1 | Hub → 玩法介绍 | 全屏手册打开（左导航 + 右正文/插图区） |
| 2.2 | 左侧导航 6 项 | 玩法概览 / 路线与章节 / 战斗与构筑 / 补给与事件 / 永久图鉴 / 解放战与提示 |
| 2.3 | 切换导航 | 标题与正文切换；可滚动长文；滚动条可用 |
| 2.4 | 插图 | 有资源则显示 banner/icon；无资源也不崩溃 |
| 2.5 | 点「关闭」 | 回到 Hub，Hub 仍可操作 |
| 2.6 | 再开玩法介绍后按返回（若游戏有返回键绑 `TryHandleBack`） | **先关手册，不关 Hub** |

---

## 3. 图鉴

| # | 操作 | 通过标准 |
|---|---|---|
| 3.1 | Hub → 图鉴 | Hub soft-hide；图鉴全屏打开 |
| 3.2 | 分类切换 | 角色 / 战斗 / 异想体 / EGO 可切换 |
| 3.3 | 条目点击 | 详情区更新（允许空库提示） |
| 3.4 | 战斗书页升级开关（若有） | 切换不崩 |
| 3.5 | 点「返回」/ 关闭 | **回到 Hub**，可继续点菜单 |
| 3.6 | 连续：图鉴 → 返回 → 玩法介绍 → 关闭 | 无黑屏、无死点、无叠两层 Hub |

---

## 4. 生命周期 / 稳定性

| # | 操作 | 通过标准 |
|---|---|---|
| 4.1 | 打开图鉴期间不要强制结束游戏 | 日志无 `ShowFromHub failed` 连环 |
| 4.2 | 玩法介绍 / 图鉴 / 选层 打开时 | 不因字体修复把 Hub 整页拆掉（上层 UI 应保持） |
| 4.3 | 关闭所有子页后退出 Hub | 图书馆可正常点击，无透明全屏挡操作 |
| 4.4 | 中文以外语言（可选） | en/kr 导航键存在且非空白 |

---

## 5. 本版已修项（验收对照）

- [x] 编译：补齐 `RMRInvitationButtonGraphic` / hover / handbook 字段  
- [x] Overlay：销毁时检查图鉴可见  
- [x] 返回键：先关玩法介绍  
- [x] 字体改善协程：上层 UI 打开时不重建 Hub  
- [x] 删除死代码 `BuildPages` 与未用导航列表  
- [x] 同步 `Localize/{cn,en,kr}` 导航与 `ui_RMR_Hub_Atlas`  

---

## 6. 已知非阻断

- Hub 外观为新「邀请票券」布局，与更早居中笔刷按钮版不同（设计变更，非回归 bug）。  
- 玩法介绍插图依赖 `ArtWorks` 键；缺图时区域偏空。  
- `RMRHubButtonHover.point` 仍未使用（仅警告，不影响点击）。

---

## 7. 失败时请回传

1. `Player.log` 中带 `[RMR` / `Exception` / `NullReference` 的段落  
2. 失败步骤编号（如上表 #）  
3. 截图（Hub / 玩法介绍 / 图鉴各一张更佳）  
